#include <bits/stdc++.h>
#define X first 
#define Y second 
#define ll long long

using namespace std;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int n;
    cin >> n;
    string s;
    cin >> s;
    for (int i=0; i<n; i++) {
        cout << s;
    }
} 